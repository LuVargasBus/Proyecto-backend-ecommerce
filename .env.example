# Servidor
PUERTO=3000
NODE_ENV=development

# PostgreSQL (local: localhost | Docker: db)
BD_HOST=localhost
BD_PORT=5432
BD_USER=root
BD_PASS=pass
BD_BD=tienda

# JWT (autenticación del panel admin)
JWT_SECRET=clave_secreta_de_firma_tp4
JWT_EXPIRES_IN=1h

# Orígenes permitidos para CORS
CORS_ORIGINS=
